version: "3.8"

services:
  stardust:
    image: vickystardust5/stardust:latest
    container_name: stardust
    ports:
      - "3000:3000"
    environment:
      - AMQP_HOST=rabbitmq
      - OLLAMA_HOST=http://ollama:11434
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
    depends_on:
      - rabbitmq
      - ollama

  rabbitmq:
    image: rabbitmq:3-management
    container_name: rabbitMQ

  ollama:
    image: ollama/ollama:latest
    container_name: stardust_inference
    environment:
      - OLLAMA_NUM_PARALLEL=2
    volumes:
      - ollama_storage:/root/.ollama

volumes:
  ollama_storage:
